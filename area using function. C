#include <stdio.h>

int area(int length, int width) {
    return length * width;
}

int main() {
    int l, w;
    printf("Enter length and width: ");
    scanf("%d %d", &l, &w);

    printf("Area = %d\n", area(l, w));
    return 0;
}